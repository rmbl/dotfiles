# ~/.tmuxinator/.yml
# you can make as many tabs as you wish...

name: branchpunch
root: ~/code/branchpunch
windows:
    - editor: vim
    - shell:
        layout: even-horizontal
        panes:
            - git pull origin master
            - rails s
    - server: ssh bp
